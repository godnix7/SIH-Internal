EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:4000
EXPO_PUBLIC_SOCKET_URL=http://10.0.2.2:4000

# Optional. Without it the trip and SOS screens render a zone summary instead of
# a Google map. Set it, then re-run `npm run prebuild` so it reaches the manifest.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
